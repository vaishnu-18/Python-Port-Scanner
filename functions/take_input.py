from typing import List, Tuple

def take_input() -> tuple[str,List[int]]:
    #Ask the user to enter an ip address
    #Store it in a variable (Str)
    #Ask the user to enter a list of ports
    #Store it in a variable (List of int)
    #Return the result
    return