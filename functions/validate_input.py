from typing import List, Tuple

def validate_input(ip_address: str, scan_input: tuple[str,List[int]]):
    #Check if it's a valid ip_address? => raise an exception otherwise
    #Check if it's a valid list of ports => raise an exception otherwise
    return