from typing import List, Tuple

def port_scanner(scan_input: tuple[str,List[int]]) -> tuple[int,bool]:
    #loop through the list of ports and for each one call port_scan(ip_address,port)
    return     